<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_page"
    android:padding="24dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="24dp">

            <ImageView
                android:id="@+id/btnBack"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:src="?attr/homeAsUpIndicator"
                app:tint="@color/text_primary"
                android:clickable="true"
                android:focusable="true"
                android:background="?attr/selectableItemBackgroundBorderless"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Select Topic"
                android:textSize="24sp"
                android:textStyle="bold"
                android:textColor="@color/text_primary"
                android:layout_marginStart="16dp"/>
        </LinearLayout>

        <Button android:id="@+id/btnMountains" style="@style/CategoryButtonStyle" android:text="🏔️  Top 10 Highest Mountains" />
        <Button android:id="@+id/btnRivers" style="@style/CategoryButtonStyle" android:text="🌊  Longest Rivers" />
        <Button android:id="@+id/btnDeserts" style="@style/CategoryButtonStyle" android:text="🌵  Largest Deserts" />
        <Button android:id="@+id/btnOceans" style="@style/CategoryButtonStyle" android:text="🐳  The 5 Oceans" />
        <Button android:id="@+id/btnWonders" style="@style/CategoryButtonStyle" android:text="🏛️  7 Wonders of the World" />
        <Button android:id="@+id/btnSolarSystem" style="@style/CategoryButtonStyle" android:text="🪐  Solar System Planets" />
        <Button android:id="@+id/btnContinents" style="@style/CategoryButtonStyle" android:text="🌍  The 7 Continents" />
        <Button android:id="@+id/btnIslands" style="@style/CategoryButtonStyle" android:text="🏝️  Largest Islands" />
        <Button android:id="@+id/btnSmallestCountries" style="@style/CategoryButtonStyle" android:text="📍  Smallest Countries" />
        <Button android:id="@+id/btnRandomCountries" style="@style/CategoryButtonStyle" android:text="🎲  Random World Countries" />

    </LinearLayout>
</ScrollView>